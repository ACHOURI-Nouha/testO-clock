// Compléter la fonction isOdd pour qu'elle retourne 'true' si le numbre nb est impair et 'false' si le nombre nb est pair
// Puis, exporter cette fonction.

/**
 * @param {number} nb 
 * @returns boolean
 * @example isOdd(3) // true
 * @example isOdd(4) // false
*/
function isOdd(nb) {
  return false;
}
